version: '3.8'
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: yuichat-frontend
    container_name: yuichat_frontend
    restart: unless-stopped
    ports:
      - "8000:80"
